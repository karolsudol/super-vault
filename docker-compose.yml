# docker-compose.yml
version: '3.8'
services:
  dbt:
    image: fishtownanalytics/dbt:1.0.0
    volumes:
      - ./dbt:/usr/app/dbt
    environment:
      DBT_PROFILES_DIR: ${DBT_PROFILES_DIR}
    command: dbt run